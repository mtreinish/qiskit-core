---
features_circuits:
  - |
    Added a Qiskit native symbolic expression implementation to replace
    the previous use of ``symengine``. This removes the dependency on an
    external library for using :class:`.ParameterExpression`.
